<% form_for :reminder, url: user_reminders, class: 'form-horizontal' do |f| %>
<%= f.collection_select :group_id, Group.where(user: @user).order(:name), :id, :name %>
<%= f.collection_select :contact_id, Contact.where(user: @user).order(:last_name), :id, :full_name %>


<% end %>