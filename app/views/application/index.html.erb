<div class="center-me">
<% if logged_in? %>

  <%= link_to 'Create Reminder', new_user_reminder_path(@user), class: "btn btn-primary" %>

<% else %>
  
  <%= link_to 'Login', new_session_url, class: "btn btn-primary" %>

<% end %>
</div>

